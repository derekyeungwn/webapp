import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import LoginSelectionContainer from '../../containers/LoginContainer/LoginSelectionContainer'

const LoginApp = ({
}) => (
  <div>
    <LoginSelectionContainer/>
  </div>
);

export default LoginApp;
